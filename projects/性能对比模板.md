# 算子性能对比记录

> 在此记录同一算子在不同实现下的性能数据，便于横向对比和优化决策。

## 填写说明

1. 复制下方的**算子对比模板**到新段落
2. 替换 `[算子名称]` 为实际算子名
3. 填写每个实现的性能数据
4. 运行环境信息填写在表格中

---

## 运行环境

| 项目 | 值 |
|------|-----|
| NPU 型号 | （如 Ascend 910B） |
| CANN 版本 | （如 8.5.0） |
| Triton-Ascend 版本 | （如 3.2.0） |
| torch_npu 版本 | （如 2.7.1） |
| PyTorch 版本 | （如 2.7.1） |
| 驱动版本 | （`npu-smi info` 输出） |
| 测试日期 | |

---

<!-- ============================================================ -->
<!-- 复制以下模板，每个算子一份 -->
<!-- ============================================================ -->

## [算子名称] 性能对比

### 算子描述

- **计算公式**: `C = A @ B`（示例，替换为实际公式）
- **输入**: A `[M, K]`, B `[K, N]`
- **输出**: C `[M, N]`
- **数据类型**: float16

### 测试参数

| 参数 | 值 |
|------|-----|
| 输入 Shape | （如 `A: [1024, 1024], B: [1024, 1024]`） |
| 数据类型 | （如 `float16`） |
| Triton BLOCK_SIZE | （如 `BLOCK_M=128, BLOCK_N=128, BLOCK_K=32`） |
| 预热次数 | 10 |
| 测试次数 | 100 |

### 性能数据

| 实现方式 | 执行时间 (ms) | 吞吐量 | TFLOPS / GB/s | 备注 |
|---------|:------------:|:------:|:-------------:|------|
| PyTorch (torch_npu) | | | | 参考基准 |
| Triton-Ascend (基础) | | | | 无 autotune |
| Triton-Ascend (autotune) | | | | 最优 Config |
| Triton-Ascend (持久化) | | | | 固定核数 |
| AscendC (如有) | | | | 手写 AscendC |
| CUDA Triton (如有) | | | | GPU 对照 |

### 加速比

以 PyTorch 为基准：

| 实现方式 | 相对 PyTorch | 相对最优 Triton |
|---------|:-----------:|:--------------:|
| PyTorch | 1.00x | |
| Triton (最优) | | 1.00x |

### Autotune 最优配置

```
最优 Config: BLOCK_M=???, BLOCK_N=???, BLOCK_K=???
```

### 数值精度

| 实现对比 | 最大绝对误差 | 平均绝对误差 | 是否通过 |
|---------|:----------:|:----------:|:-------:|
| Triton vs PyTorch | | | （atol<? / rtol<?） |

### 备注

（记录任何特殊情况，如 UB 溢出、对齐问题、调优发现等）

---

<!-- ============================================================ -->
<!-- 以下是示例填写，供参考 -->
<!-- ============================================================ -->

## Vector Add 性能对比（示例）

### 算子描述

- **计算公式**: `C = A + B`
- **输入**: A `[N]`, B `[N]`
- **输出**: C `[N]`
- **数据类型**: float32

### 测试参数

| 参数 | 值 |
|------|-----|
| 输入 Shape | `N = 10,000,000` |
| 数据类型 | float32 |
| Triton BLOCK_SIZE | 1024 |
| 预热次数 | 10 |
| 测试次数 | 100 |

### 性能数据

| 实现方式 | 执行时间 (ms) | 带宽 (GB/s) | 备注 |
|---------|:------------:|:-----------:|------|
| PyTorch (x + y) | 0.052 | 219.3 | `torch.add` |
| Triton-Ascend | 0.048 | 237.5 | BLOCK_SIZE=1024 |

### 加速比

| 实现方式 | 相对 PyTorch |
|---------|:-----------:|
| PyTorch | 1.00x |
| Triton-Ascend | 1.08x |

### 数值精度

| 实现对比 | 最大绝对误差 | 是否通过 |
|---------|:----------:|:-------:|
| Triton vs PyTorch | 0.0 | PASS (atol=1e-5) |

---

## Matmul 性能对比（示例）

### 算子描述

- **计算公式**: `C = A @ B`
- **输入**: A `[1024, 1024]`, B `[1024, 1024]`
- **输出**: C `[1024, 1024]`
- **数据类型**: float16

### 测试参数

| 参数 | 值 |
|------|-----|
| 输入 Shape | `A: [1024, 1024], B: [1024, 1024]` |
| 数据类型 | float16 |
| Triton BLOCK_SIZE | autotune |
| 预热次数 | 10 |
| 测试次数 | 100 |

### 性能数据

| 实现方式 | 执行时间 (ms) | TFLOPS | 备注 |
|---------|:------------:|:------:|------|
| PyTorch (torch.matmul) | 0.31 | 6.96 | NPU cublas |
| Triton-Ascend (autotune) | 0.35 | 6.17 | BLOCK=128x128x32 |

### 加速比

| 实现方式 | 相对 PyTorch |
|---------|:-----------:|
| PyTorch | 1.00x |
| Triton-Ascend | 0.89x |

### Autotune 最优配置

```
最优 Config: BLOCK_M=128, BLOCK_N=128, BLOCK_K=32
（从 7 个候选 Config 中自动选出）
```

### 数值精度

| 实现对比 | 最大绝对误差 | 是否通过 |
|---------|:----------:|:-------:|
| Triton vs PyTorch | 0.023 | PASS (atol=0.05, float16) |

---

## Softmax 性能对比（待填写）

### 算子描述

- **计算公式**: `softmax(x) = exp(x - max(x)) / sum(exp(x - max(x)))`
- **输入**: x `[M, N]`
- **输出**: y `[M, N]`
- **数据类型**: float16

### 测试参数

| 参数 | 值 |
|------|-----|
| 输入 Shape | `x: [4096, 4096]` |
| 数据类型 | float16 |
| Triton BLOCK_SIZE | |
| 预热次数 | 10 |
| 测试次数 | 100 |

### 性能数据

| 实现方式 | 执行时间 (ms) | 带宽 (GB/s) | 备注 |
|---------|:------------:|:-----------:|------|
| PyTorch (torch.softmax) | | | |
| Triton-Ascend | | | |

### 加速比

| 实现方式 | 相对 PyTorch |
|---------|:-----------:|
| PyTorch | 1.00x |
| Triton-Ascend | |

### 数值精度

| 实现对比 | 最大绝对误差 | 是否通过 |
|---------|:----------:|:-------:|
| Triton vs PyTorch | | |

---

## Layer Norm 性能对比（待填写）

### 算子描述

- **计算公式**: `y = (x - mean) / sqrt(var + eps) * gamma + beta`
- **输入**: x `[M, N]`, gamma `[N]`, beta `[N]`
- **输出**: y `[M, N]`
- **数据类型**: float16

### 测试参数

| 参数 | 值 |
|------|-----|
| 输入 Shape | `x: [4096, 1024]` |
| 数据类型 | float16 |
| Triton BLOCK_SIZE | |
| eps | 1e-5 |
| 预热次数 | 10 |
| 测试次数 | 100 |

### 性能数据

| 实现方式 | 执行时间 (ms) | 带宽 (GB/s) | 备注 |
|---------|:------------:|:-----------:|------|
| PyTorch (torch.nn.LayerNorm) | | | |
| Triton-Ascend | | | |

### 加速比

| 实现方式 | 相对 PyTorch |
|---------|:-----------:|
| PyTorch | 1.00x |
| Triton-Ascend | |

### 数值精度

| 实现对比 | 最大绝对误差 | 是否通过 |
|---------|:----------:|:-------:|
| Triton vs PyTorch | | |
